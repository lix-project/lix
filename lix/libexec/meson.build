kill_user = executable(
  'kill-user',
  files('kill-user.cc'),
  install : true,
  install_dir : libexecdir / 'lix',
)

run_pager = executable(
  'run-pager',
  files('run-pager.cc'),
  install : true,
  install_dir : libexecdir / 'lix',
)
