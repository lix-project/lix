[package]
name = "lix"
edition = "2021"

[lib]
name = "lixrs"
path = "src/lib.rs"
crate-type = ["staticlib"]

[features]
default = []

[dependencies]
lix-doc.path = "../lix-doc"

[build-dependencies]
zngur.workspace = true
